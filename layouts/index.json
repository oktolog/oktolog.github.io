{{- $pages := .Site.RegularPages -}}
[{{ range $i, $page := $pages }}{{ if $i }},{{ end }}{"title":{{ $page.Title | jsonify }},"url":{{ $page.Permalink | jsonify }},"description":{{ $page.Description | jsonify }},"tags":{{ $page.Params.tags | jsonify }}}{{ end }}]
